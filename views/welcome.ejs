<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>

        *{
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        .container{
            width: 500px;
            margin: 100px auto 10px;
        }

        input{
            width: 100%;
            box-sizing: border-box;
            padding: 8px 10px;
        }

        .btn{
            margin-top: 12px;
            padding: 8px 26px;
            background-color: black;
            color: white;
            font-size: 16px;
            border: 0;
            border-radius: 4px;
        }

        .btn:active{
            transform: scale(1.02);
        }

        .task{
            border: 1px solid rgb(152, 152, 152);
            padding: 8px 20px;
            border-radius: 4px;
            margin-bottom: 8px;
        }

        .flex{
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .btnx{
            margin-top: 15px;
            padding: 8px 18px;
            font-size: 16px;
            font-size: 16px;
            font-weight: 600;
            background-color:#292929;
            border: 2px solid #292929;
            color: white;
            border-radius: 4px;
            transition: 1s;
        }

        .btnx:hover{
            border: 2px solid black;
            background-color: white;
            color: black;
        }

        .gap{
            gap: 18px;
        }

        nav{
            width: 90%;
            margin: auto;
        }

        .logo{
            color: rgb(73, 73, 73);
            font-size: 30px;
            font-weight: bolder;
            margin-left: 10px;
        }

        /* #display-text{
            color: rgb(100, 0, 193);
        } */

    </style>

</head>

<body>

    <nav class="flex">
        <div class="logo">TodoList</div>

        <div class="flex gap">

            <form action="/register">
                <button class="btnx" type="submit">sign up</button>
            </form>

            <form action="/login">
                <button class="btnx" type="submit">log in</button>
            </form>

        </div>


    </nav>


    

    <div class="container">


        <h1 id="display-text"> </h1>
        <p>Lorem ipsum dolor sit, amet consectetur adipisicing elit. Hic nemo quisquam quibusdam corporis vero ipsam explicabo minus nobis?</p>
        
       
    </div>

</body>

<script>
        const text = "Login to Manage your Task!";
        let index = 0;

        function displayCharacterByCharacter() {
            // Append the next character to the display-text div
            document.getElementById('display-text').textContent += text.charAt(index);
            index++;

            // If the entire sentence has been displayed, reset the index after a delay
            if (index < text.length) {
                setTimeout(displayCharacterByCharacter, 100); // Adjust delay as needed
            } else {
                setTimeout(() => {
                    document.getElementById('display-text').textContent = "";
                    index = 0;
                    displayCharacterByCharacter();
                }, 2000); // Delay before repeating
            }
        }

        // Start the process
        displayCharacterByCharacter();
    </script>


</html> 



 

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="../app.js"></script>

    <style>
        * {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        h1 {
            margin-bottom: 0px;
            font-size: 30px;
        }

        p {
            margin-top: 10px;
        }

        .container {
            width: 400px;
            margin: 20px auto 10px;
        }

        input {
            width: 100%;
            box-sizing: border-box;
            padding: 8px 10px;
            font-size: 16px;
            border: 1.5px solid gray;
            border-radius: 4px;

        }

        .btn {
            margin-top: 4px;
            padding: 12px 26px;
            background-color: #292929;
            color: white;
            font-size: 16px;
            border: 0;
            border-radius: 4px;
        }

        .btn:active {
            transform: scale(1.02);
        }

        .task {
            border: 1px solid rgb(152, 152, 152);
            padding: 8px 20px;
            border-radius: 4px;
            margin-bottom: 8px;
        }

        .flex {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .btnx {
            margin-top: 15px;
            padding: 8px 18px;
            font-size: 16px;
            font-size: 16px;
            font-weight: 600;
            background-color: white;
            border: 2px solid rgb(189, 1, 1);
            color: rgb(189, 1, 1);
            border-radius: 4px;
            transition: 1s;
        }

        .btnx:hover {
            border: 2px solid rgb(189, 1, 1);
            background-color: rgb(189, 1, 1);
            color: white;
        }

        .gap {
            gap: 18px;
        }

        nav {
            width: 90%;
            margin: auto;
        }

        .logo {
            color: rgb(73, 73, 73);
            font-size: 30px;
            font-weight: bolder;
            margin-left: 10px;
        }

        input {
            margin-bottom: 12px;
        }

        .mt0 {
            margin-top: 10px;
        }

        .show {
            width: 450px;
            margin: 30px auto 10px;
        }

        p {
            margin-bottom: 22px;
        }

        h1 {
            font-size: 28px;
        }

        span {
            color: gray;
        }

        table {
            width: 100%;
            margin: 50px auto;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            font-weight: 700;
            font-size: 16px;
        }

        .submit {
            color: red;
            background: transparent;
            border: 0;
            padding: 0;
            font-size: 16px;
        }
    </style>

</head>

<body>


    <nav class="flex">
        <div class="logo">TodoList</div>

        <div class="flex gap">

            <form action="/do-logout">
                <button class="btnx" type="submit">log out</button>
            </form>

        </div>
    </nav>


    <div class="container">

        <h1>Manage All user <span>@<%= name %></span></h1>
        <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Earum iure iste illo?</p>
    </div>

    <div class="show">

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>

                <% for(let i=0; i < users.length ; i++){ %>
                    <tr>
                        <th scope="row">
                            <%=i+1%>
                        </th>
                        <td>
                            <%= users[i].name %>
                        </td>
                        <td>
                            <%= users[i].email %>
                        </td>
                        <td>
                            <form action="/delete-user" method="post">
                                <input type="text" name="delEmail" type="email" value='<%= users[i].email %>' hidden>
                                <button class="submit" type="submit">delete</button>
                            </form>
                        </td>
                    </tr>
                    <%}%>
            </tbody>
        </table>

    </div>

</body>

</html>